$(function(e) {     
    $('#example').DataTable({         
        paging: false, 
        scrollY: "400px", 
        scrollX: true,  // Prevent horizontal scrolling
        autoWidth: false,  // Adjust columns dynamically
        fixedHeader: true,
        columnDefs: [
            { width: "auto", targets: "_all" } // Applies auto width to all columns
        ]
    }); 
});
$(document).ready(function() {
	$('#example2').DataTable();
} );